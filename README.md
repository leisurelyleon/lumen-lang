# lumen-lang
A tree-walking interpreter for Lumen, a small dynamically-typed language, written in Rust. Implements the full pipeline: a hand-written lexer, a recursive-descent parser with correct operator precedence, and a tree-walking evaluator supporting variables, control flow, and first-class functions with lexical closures.
