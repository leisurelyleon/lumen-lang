// Recursive Fibonacci: prints fib(0) through fib(9).
fn fib(n) {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

var i = 0;
while (i < 10) {
    print fib(i);
    i = i + 1;
}
